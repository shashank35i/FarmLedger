<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fl_surface_variant"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- HEADER -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:contentDescription="Back"
                android:src="@drawable/ic_back" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select Retailer"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/fl_on_surface"
                android:layout_marginStart="12dp" />
        </LinearLayout>

        <!-- TRANSFER INFO CARD -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/card_bg"
            android:padding="16dp"
            android:layout_marginBottom="20dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Transfer Batch to Retailer"
                android:textStyle="bold"
                android:textColor="@color/fl_on_surface"
                android:textSize="14sp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Select a verified retailer to transfer Organic Tomatoes. Once transferred, batch data will be updated."
                android:textSize="13sp"
                android:textColor="@color/fl_on_surface_variant"
                android:layout_marginTop="6dp" />
        </LinearLayout>

        <!-- VERIFIED RETAILERS TITLE -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Verified Retailers"
            android:textStyle="bold"
            android:textSize="14sp"
            android:textColor="@color/fl_on_surface"
            android:layout_marginBottom="12dp" />

        <!-- RETAILER LIST CONTAINER -->
        <LinearLayout
            android:id="@+id/retailerListContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <!-- MANUAL ENTRY -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Or Enter Manually"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/fl_on_surface"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="8dp" />

        <EditText
            android:id="@+id/etManualRetailer"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Enter Retailer ID or Phone Number"
            android:padding="12dp"
            android:background="@drawable/bg_input"
            android:textSize="14sp" />

        <!-- CONFIRM BUTTON -->
        <Button
            android:id="@+id/btnConfirmTransferRetailer"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="Confirm Transfer"
            android:textColor="@color/fl_on_surface"
            android:backgroundTint="@color/fl_tertiary"
            android:layout_marginTop="24dp" />

        <!-- CANCEL BUTTON -->
        <Button
            android:id="@+id/btnCancel"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="Cancel"
            android:backgroundTint="@color/fl_surface"
            android:textColor="@color/fl_primary"
            android:layout_marginTop="12dp" />

        <!-- FRAGMENT CONTAINER -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="gone" />
    </LinearLayout>
</ScrollView>
